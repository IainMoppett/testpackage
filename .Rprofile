## Print this on start so I know it's loaded.
cat("Loading custom .Rprofile")
## A little gem from Hadley Wickham to automatically load devtools in interactive sessions.

if (interactive()) {
  suppressMessages(require(devtools))
}
